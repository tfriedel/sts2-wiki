# Copy to .env (gitignored) and adjust per-machine.

# Base path the site is served under. Set automatically by `just build-all-versions`.
# ASTRO_BASE=/sts2-wiki/

# Comma-separated extra hostnames `astro dev` will accept (LAN host, ngrok, etc.).
# ASTRO_ALLOWED_HOSTS=mybox,my-tunnel.ngrok.io
